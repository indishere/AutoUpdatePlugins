name: AutoUpdatePlugins
version: '${project.version}'
main: io.github.ind.autoupdateplugins.AutoUpdatePlugins
api-version: '1.16'
folia-supported: true
prefix: AUP
author: IND

commands:
  aup:
    description: Use the plugin commands
    permission: aup.command

permissions:
  aup.command:
    description: Use the plugin commands
    default: op
